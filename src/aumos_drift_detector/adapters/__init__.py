"""Adapters — concrete infrastructure implementations.

Bridges the domain core to PostgreSQL (repositories), Kafka (event publisher),
scipy/scikit-learn (statistical tests), and streaming drift detectors.
"""
